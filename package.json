{
  "name": "elm-wendler",
  "version": "1.0.0",
  "description": "wendler 531 program tracker in elm",
  "scripts": {
    "start": "parcel app/index.html",
    "build": "parcel build app/index.html --public-url ./",
    "test": "elm-test",
    "autotest": "elm-test --watch",
    "deploy": "rm -rf docs && mkdir docs && cp dist/* docs/"
  },
  "author": "Eric Lifka",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "elm": "^0.19.0-bugfix2",
    "elm-hot": "^1.0.1",
    "elm-test": "^0.19.0-beta9",
    "node-elm-compiler": "^5.0.1",
    "parcel-bundler": "^1.10.1"
  }
}